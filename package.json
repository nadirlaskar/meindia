{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "docker:build": "lerna run docker:build"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "lerna": "^7.1.4"
  }
}
