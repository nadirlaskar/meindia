{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "docker:base:build": "lerna run docker:base:build",
    "docker:build": "lerna run docker:build",
    "docker:start-all": "docker-compose up -d ",
    "update:dev": "NODE_ENV=development npm run docker:build && run docker:start-all"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "lerna": "^7.1.4"
  }
}